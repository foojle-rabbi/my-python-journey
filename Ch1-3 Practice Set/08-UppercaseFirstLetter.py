upperCase = input("Enter a string: ")
print("Uppercasing the first latter : ", upperCase.capitalize()) 
# capitalize is a methods of string that use to capitalize the first char of any string. Note: It wont work as puntuator!

# code of gpt
upperCase = input("Enter a string: ")
print("Uppercasing the first letter:", upperCase.capitalize()) 
# capitalize() capitalizes only the first character of the entire string 
# and converts the rest to lowercase. It does not capitalize letters after punctuation inside the string.

upperCase = input("Enter a string: ")
print(upperCase.title()) # if you want to make uppercase of each word then use str.tittle()

